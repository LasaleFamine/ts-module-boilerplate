{
  "name": "ts-module-boilerplate",
  "version": "0.0.0",
  "description": "",
  "keywords": [
    ""
  ],
  "license": "MIT",
  "repository": "GITHUB_USERNAME/ts-module-boilerplate",
  "author": {
    "name": "YOUR_NAME",
    "email": "YOUR_EMAIL",
    "url": "YOUR_WEBSITE"
  },
  "scripts": {
    "build": "del-cli dist && tsc",
    "prepack": "npm run build",
    "test": "tsc --sourceMap && eslint . && nyc ava"
  },
  "engines": {
    "node": ">=10"
  },
  "dependencies": {},
  "devDependencies": {},
  "files": [
    "dist/src",
    "!*.test.*"
  ],
  "main": "dist",
  "types": "dist",
  "nyc": {
    "all": true,
    "extension": [
      ".ts"
    ],
    "include": [
      "dist/src",
      "src"
    ],
    "exclude": [
      "**/*.d.ts",
      "**/*.test.ts"
    ],
    "reporter": [
      "lcov",
      "text"
    ]
  }
}
