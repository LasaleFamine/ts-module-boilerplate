import { moduleName } from '../';

describe('when do something', () => {
  it('should comply to something else', () => {
    const res = moduleName('something');
    expect(res).toBe('something else');
  })
})
